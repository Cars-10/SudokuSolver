# Vimscript Sudoku Solver

## Requirements

```bash
apt install vim
```

## Notes

Vimscript (also known as VimL) is the scripting language built into the Vim text editor. The solver runs Vim in Ex mode (`-es`) to execute the script without launching the full editor UI.
