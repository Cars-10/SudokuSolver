# Zsh Sudoku Solver

## Requirements

```bash
apt install zsh
```

## Notes

Zsh (Z shell) is a powerful Unix shell with many features for interactive use and scripting. The solver demonstrates Zsh's array handling and arithmetic capabilities.
