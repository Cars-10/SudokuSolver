# C++ Sudoku Solver

## Validation Status

✅ **FULLY VALIDATED** - 2025-12-18

| Matrix | Expected Iterations | Actual | Status |
|--------|--------------------:|-------:|:------:|
| 1      | 656                 | 656    | ✓ PASS |
| 2      | 439,269             | 439,269| ✓ PASS |
| 3      | 98,847              | 98,847 | ✓ PASS |
| 4      | 9,085               | 9,085  | ✓ PASS |
| 5      | 445,778             | 445,778| ✓ PASS |

**Format Validation:** ✅ All matrices pass (output format matches C reference exactly)

## Algorithm

This is an exact port of the C reference implementation. The algorithm is pure brute-force recursive backtracking:

- **Search order:** Row-major (top-to-bottom, left-to-right)
- **Candidate values:** 1-9 in ascending order
- **Iteration counting:** Every value placement attempt is counted
- **Validation:** Row, column, and 3x3 box constraints checked

The iteration counts are the algorithm fingerprint - they prove exact algorithmic match with the C reference.

## Compilation

### Default (used in benchmarks)
```bash
g++ -O3 -o Sudoku Sudoku.cpp
```

### Compiler Variants

| Variant | Flags    | Description                          |
|---------|----------|--------------------------------------|
| O0      | `-O0`    | No optimization (debugging)          |
| O1      | `-O1`    | Basic optimization                   |
| O2      | `-O2`    | Moderate optimization                |
| **O3**  | **`-O3`**| **Aggressive optimization (default)**|
| Ofast   | `-Ofast` | Maximum optimization (may break IEEE)|
| Os      | `-Os`    | Optimize for size                    |

**Usage:**
```bash
# Default O3
./runMe.sh

# Custom variant
VARIANT=Ofast ./runMe.sh
```

## Execution

### Run with modular script (recommended)
```bash
cd Algorithms/BruteForce/C++
./runMe.sh /app/Matrices/1.matrix
```

### Run all matrices
```bash
./runMe.sh /app/Matrices/{1,2,3,4,5}.matrix
```

### Direct execution
```bash
g++ -O3 -o Sudoku Sudoku.cpp
./Sudoku /app/Matrices/1.matrix
```

## Performance

Validated performance on Docker (ubuntu:24.04, g++ -O3):

| Matrix | Iterations | Time (s) | Memory (KB) |
|--------|----------:|----------:|------------:|
| 1      | 656        | ~0.001    | ~1,372      |
| 2      | 439,269    | ~0.002    | ~1,384      |
| 3      | 98,847     | ~0.001    | ~1,384      |
| 4      | 9,085      | ~0.000    | ~1,384      |
| 5      | 445,778    | ~0.001    | ~1,380      |

**Notes:**
- Times vary by CPU - iteration counts are canonical
- All matrices complete well under 5-minute timeout
- Memory usage is constant (stack-based recursion)

## Files

| File         | Purpose                                    |
|--------------|--------------------------------------------|
| Sudoku.cpp   | Main solver implementation                 |
| runMe.sh     | Modular benchmark script (uses common.sh)  |
| README.md    | This file (overview and usage)             |
| metrics.json | Benchmark results (generated by runMe.sh)  |

## Code Structure

Key functions in Sudoku.cpp:

```cpp
int isValid(int row, int col, int val)  // Check if placement is valid
int solve()                              // Recursive brute-force solver
int readMatrixFile(char *filename)       // Load puzzle from .matrix file
void printPuzzle()                       // Display current puzzle state
```

## Output Format

The C++ solver produces output matching the C reference exactly:

```
../Matrices/1.matrix
9 2 0 0 0 0 5 8 4
... (initial puzzle rows as read)

Puzzle:
9 2 0 0 0 0 5 8 4
... (initial puzzle with header)

Puzzle:
9 2 1 6 3 7 5 8 4
... (solved puzzle)

Solved in Iterations=656

Seconds to process 0.000
```

## Validation Commands

```bash
# Run and validate in Docker
cd Algorithms/BruteForce/C++
./runMe.sh /app/Matrices/{1,2,3,4,5}.matrix
```
