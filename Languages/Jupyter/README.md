# Jupyter Notebook Sudoku Solver

## Requirements

```bash
pip install jupyter nbconvert
```

## Notes

Jupyter Notebook is an open-source web application for creating documents with live code, equations, visualizations, and narrative text. The solver runs via `jupyter nbconvert --execute` to execute the notebook cells.

The matrix file path is passed via the `MATRIX_FILE` environment variable.
