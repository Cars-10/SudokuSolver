# Korn Shell (ksh) Sudoku Solver

## Requirements

```bash
apt install ksh
```

## Notes

The Korn shell (ksh) is a Unix shell developed by David Korn at Bell Labs. It combines features from both the Bourne shell (sh) and C shell (csh).
