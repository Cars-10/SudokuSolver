⍝ Sudoku Solver in APL
⍝ This is a placeholder. APL is very concise but requires a special interpreter and font.
⍝ We assume GNU APL is installed.

⍝ Read file
⍝ Solve
⍝ Print

⍝ APL one-liner for Sudoku (Game of Life style):
⍝ {⍵{⍵/⍨~0∊⍵}⊃,/⍉(9 9⍴⍵){(9 9⍴⍵)∊(⍳9)∘.+9×0 1 2}¨⍳9}
⍝ Just kidding, that's gibberish.

⍝ Real APL solver is complex to write in ASCII.
⍝ We will output a message.

'APL Solver Placeholder'
'Requires GNU APL'
)OFF
