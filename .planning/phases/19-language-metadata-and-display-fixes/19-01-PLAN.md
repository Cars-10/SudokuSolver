---
phase: 19-language-metadata-and-display-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [Metrics/LanguagesMetadata.ts]
autonomous: true
---

<objective>
Restore complete language metadata structure with author information and images.

Purpose: Fix missing language metadata and author pictures by extending the LanguageMeta interface and populating structured author data for all languages.
Output: Complete LanguageMeta interface with authors array, populated metadata for all ~88 languages with author names and images where available.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-validation-and-integration/18-PHASE-SUMMARY.md
@Metrics/LanguagesMetadata.ts
@Metrics/types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend LanguageMeta interface with author fields</name>
  <files>Metrics/LanguagesMetadata.ts</files>
  <action>
Update the LanguageMeta interface (currently lines 174-185) to include:
- `authors?: Array<{ name: string; image?: string; website?: string }>` - structured author information
- `image?: string` - primary language logo/image
- `website?: string` - official language website

This aligns the interface with what the UI client (report_client.js lines 651-668) expects when rendering the language metadata modal.

Do NOT remove existing fields (creator, date, description, location, benefits, related, history, paradigm, typeSystem) - these are still used elsewhere in the codebase.
  </action>
  <verify>TypeScript compilation passes: npx tsc --noEmit in Metrics/ directory</verify>
  <done>LanguageMeta interface includes authors array, image, and website fields</done>
</task>

<task type="auto">
  <name>Task 2: Populate authors data for all languages</name>
  <files>Metrics/LanguagesMetadata.ts</files>
  <action>
For each entry in the languageMetadata object (starting ~line 187), add the authors array field where creators are known.

Strategy:
1. Parse existing `creator` field (e.g., "Dennis Ritchie" or "Aho, Weinberger, Kernighan")
2. Create authors array with individual entries: `authors: [{ name: "Dennis Ritchie" }]`
3. For multiple creators (comma-separated), split into separate author objects
4. Add `image` field at language level for primary logo (where applicable - leave empty for now, can be populated later)
5. Add `website` field for official language sites (where known)

Focus on ~20-30 most prominent languages first (C, C++, Python, JavaScript, Java, Rust, Go, Ruby, etc.). For less common languages, the authors array can be populated incrementally.

Example structure:
```typescript
"C": {
    creator: "Dennis Ritchie",
    date: "1972",
    authors: [
        { name: "Dennis Ritchie", image: "", website: "" }
    ],
    image: "",
    website: "https://www.iso.org/standard/74528.html",
    description: "...",
    // ... rest of fields
}
```

For languages with multiple creators like AWK:
```typescript
"Awk": {
    creator: "Aho, Weinberger, Kernighan",
    authors: [
        { name: "Alfred Aho" },
        { name: "Peter Weinberger" },
        { name: "Brian Kernighan" }
    ],
    // ... rest
}
```

Do NOT break existing `creator` field - it's still referenced by the UI for backwards compatibility.
  </action>
  <verify>
1. npx tsc --noEmit succeeds
2. Spot check 10 languages have authors array: grep -A 2 'authors:' Metrics/LanguagesMetadata.ts | head -30
3. No syntax errors in the metadata object
  </verify>
  <done>At least 20-30 major languages have populated authors arrays with structured data</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] TypeScript compilation passes (npx tsc --noEmit)
- [ ] LanguageMeta interface includes authors, image, website fields
- [ ] Major languages (C, Python, JavaScript, etc.) have authors arrays populated
- [ ] No breaking changes to existing metadata structure
</verification>

<success_criteria>

- All tasks completed
- Interface extended without breaking existing code
- Authors data populated for prominent languages
- No TypeScript compilation errors
  </success_criteria>

<output>
After completion, create `.planning/phases/19-language-metadata-and-display-fixes/19-01-SUMMARY.md`
</output>
