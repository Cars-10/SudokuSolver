---
phase: 10-scripting-languages-part-2
plan: 02
type: execute
depends_on: []
files_modified: [Algorithms/DLX/Lua/dlx.lua, Algorithms/DLX/Lua/runMe.sh, Algorithms/CP/Lua/cp.lua, Algorithms/CP/Lua/runMe.sh]
---

<objective>
Implement DLX and CP algorithms for Lua, extending algorithm coverage to this lightweight embeddable scripting language.

Purpose: Port Dancing Links (DLX) and Constraint Propagation (CP) algorithms from C reference to Lua, following established patterns. Lua's tables provide flexible structures for DLX nodes.
Output: Working DLX and CP implementations for Lua with verified iteration counts (DLX: 43, CP: 67).
</objective>

<execution_context>
./.claude/get-shit-done/workflows/execute-plan.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@Algorithms/DLX/C/dlx_core.c
@Algorithms/CP/C/cp_core.c
@Algorithms/BruteForce/Lua/Sudoku.lua

**Tech stack available**: Lua (already present for BruteForce)
**Established patterns**: Mechanical translation from C, iteration count verification (DLX: 43, CP: 67), runMe.sh integration
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement DLX (Dancing Links) algorithm in Lua</name>
  <files>Algorithms/DLX/Lua/dlx.lua, Algorithms/DLX/Lua/runMe.sh</files>
  <action>
Create Algorithms/DLX/Lua/ directory structure. Port the DLX algorithm from C to Lua:

**dlx.lua structure**:
- Use Lua tables for DlxNode: {left = ref, right = ref, up = ref, down = ref, column = ref, row_id = num}
- Use Lua tables for DlxColumn: {node = node_ref, size = num, name = str}
- Implement function cover_column(c) - exact same logic as C (unlink column and all rows)
- Implement function uncover_column(c) - exact reversal of cover operation
- Implement function search(root, k, solution) - recursive backtracking with Algorithm X
- Implement function build_exact_cover_matrix() - create 324 columns for Sudoku constraints
- Implement function parse_matrix(filename) - read .matrix file and create constraint rows
- Main execution that calls parse_matrix, search, and prints results
- Global dlx_iterations variable incremented at start of search

**Critical requirements**:
- Use Lua tables as object-like structures (Lua's native data structure)
- Maintain circular doubly-linked list structure exactly as in C
- Print output format: puzzle state, then "Solved in Iterations=43"
- Use same exact cover mapping: row*81 + col*9 + digit
- File structure: all code in single dlx.lua file, executable via lua dlx.lua <matrix_file>
- Add shebang: #!/usr/bin/env lua
- Lua arrays are 1-indexed - adjust all array indexing accordingly

**runMe.sh**:
- Set LANGUAGE="Lua", SOLVER_BINARY="lua dlx.lua", METRICS_FILE="metrics.json"
- Source ../../common.sh
- compile() function should check_toolchain lua and verify syntax (lua doesn't have -l flag, just check executable exists)
- Call main "$@"

**What to avoid and WHY**:
- Do NOT use metatables unless necessary - plain tables are sufficient
- Do NOT use Lua's table library for node manipulation - need explicit pointer operations
- Do NOT optimize the search order - must match C reference exactly
- Do NOT use LuaRocks packages - keep implementation self-contained
- Do NOT forget Lua uses 1-based indexing - adjust all loops and array accesses
</action>
  <verify>cd Algorithms/DLX/Lua && ./runMe.sh ../../../Matrices/1.matrix shows "Solved in Iterations=43"</verify>
  <done>DLX implementation runs cleanly, produces exactly 43 iterations for Matrix 1, metrics.json created</done>
</task>

<task type="auto">
  <name>Task 2: Implement CP (Constraint Propagation) algorithm in Lua</name>
  <files>Algorithms/CP/Lua/cp.lua, Algorithms/CP/Lua/runMe.sh</files>
  <action>
Create Algorithms/CP/Lua/ directory structure. Port the CP algorithm from C to Lua:

**cp.lua structure**:
- Use Lua table for CPGrid: {values = {{...}}, candidates = {{...}}} (2D arrays for bitsets)
- Define bitset helper functions:
  - function has_candidate(set, digit) - test bit
  - function add_candidate(set, digit) - set bit (returns new value)
  - function remove_candidate(set, digit) - clear bit (returns new value)
  - function count_candidates(set) - popcount
- Implement function init_grid(grid, puzzle) - set empty cells to candidates = 0x1FF
- Implement function assign(grid, row, col, value) - place value and propagate constraints
- Implement function eliminate(grid, row, col, candidates_to_remove) - remove candidates and propagate
- Implement function propagate(grid) - apply constraint propagation until quiescence
- Implement function find_mrv_cell(grid) - find cell with Minimum Remaining Values (returns row, col or nil)
- Implement function cp_search(grid) - recursive search with propagation
- Implement function parse_matrix(filename) and main execution with global cp_iterations counter
- Add shebang: #!/usr/bin/env lua
- Lua arrays are 1-indexed - adjust all array indexing accordingly

**Critical requirements**:
- Use Lua numbers for bitsets (Lua numbers are double-precision floats but bit operations work correctly)
- Bitset operations: use bit32 library (Lua 5.2+) or manual bit operations for older versions
- Print output format: puzzle state, then "Solved in Iterations=67"
- Increment cp_iterations at start of cp_search
- Lua tables are passed by reference by default
- Custom popcount implementation

**runMe.sh**:
- Set LANGUAGE="Lua", SOLVER_BINARY="lua cp.lua", METRICS_FILE="metrics.json"
- Source ../../common.sh
- compile() function should check_toolchain lua
- Call main "$@"

**What to avoid and WHY**:
- Do NOT use Lua tables for candidates - must use bitsets (numbers)
- Do NOT add extra constraint propagation techniques
- Do NOT change MRV selection order
- Do NOT use LuaRocks packages - keep implementation self-contained
- Do NOT forget Lua uses 1-based indexing for arrays
</action>
  <verify>cd Algorithms/CP/Lua && ./runMe.sh ../../../Matrices/1.matrix shows "Solved in Iterations=67"</verify>
  <done>CP implementation runs cleanly, produces exactly 67 iterations for Matrix 1, metrics.json created</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] DLX Lua shows exactly 43 iterations for Matrix 1
- [ ] CP Lua shows exactly 67 iterations for Matrix 1
- [ ] Both produce correctly solved puzzles
- [ ] metrics.json files exist for both
- [ ] No Lua syntax errors
- [ ] 1-based indexing handled correctly
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Iteration counts match C reference exactly
- Both implementations integrated into benchmark system
</success_criteria>

<output>
After completion, create `.planning/phases/10-scripting-languages-part-2/10-02-SUMMARY.md`:

# Phase 10 Plan 2: Lua Algorithms Summary

**[Substantive one-liner - what shipped, not "phase complete"]**

## Accomplishments

- [Key outcome 1]
- [Key outcome 2]

## Files Created/Modified

- `path/to/file.lua` - Description
- `path/to/another.lua` - Description

## Decisions Made

[Key decisions and rationale, or "None"]

## Issues Encountered

[Problems and resolutions, or "None"]

## Next Step

Ready for Plan 10-03: R Algorithms
</output>
