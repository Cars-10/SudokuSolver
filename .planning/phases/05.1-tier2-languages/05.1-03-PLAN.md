---
phase: 05.1-tier2-languages
plan: 03
type: execute
---

<objective>
Implement Groovy and Dart Sudoku solvers with exact algorithm match and C reference output format.

Purpose: Add JVM scripting and Flutter ecosystem languages to the Tier 2 benchmark suite
Output: Working Groovy and Dart solvers passing all 5 matrix validations with exact iteration counts
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

**Prior phases establishing pattern:**
@.planning/phases/05-jvm-modern-languages/05-02-SUMMARY.md (Java pattern)

**Reference implementation:**
@Languages/C/Sudoku.c
@Languages/Java/Sudoku.java (JVM pattern)

**Existing code directories:**
@Languages/Groovy/
@Languages/Dart/

**Reference iteration counts:**
- Matrix 1: 656
- Matrix 2: 439,269
- Matrix 3: 98,847
- Matrix 4: 9,085
- Matrix 5: 445,778

**Compilation notes:**
- Groovy: groovy command or groovyc + java, JVM-based scripting
- Dart: dart run or dart compile exe, Flutter ecosystem
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Groovy solver</name>
  <files>Languages/Groovy/Sudoku.groovy, Languages/Groovy/runMe.sh, Languages/Groovy/README.md</files>
  <action>
    1. Create/fix Sudoku.groovy:
       - Groovy is JVM scripting with Java interop
       - Use int[][] puzzle = new int[9][9]
       - int count = 0 for iterations
       - def readMatrixFile(String filename)
       - def printPuzzle()
       - def isValid(int row, int col, int val)
       - def solve() - brute-force backtracking
         * count++ for every attempt
       - Use System.nanoTime() for timing

    2. Create runMe.sh:
       - LANGUAGE="Groovy"
       - SOLVER_BINARY="groovy Sudoku.groovy"
       - Source ../common.sh
       - check_toolchain groovy

    3. Create README.md with validation table
  </action>
  <verify>
    cd Languages/Groovy && groovy Sudoku.groovy ../../Matrices/1.matrix | grep "Iterations=656"
  </verify>
  <done>Groovy solver matches C reference output format and iteration count (656) for Matrix 1</done>
</task>

<task type="auto">
  <name>Task 2: Validate Groovy on all matrices</name>
  <files>Languages/Groovy/metrics.json</files>
  <action>
    1. Run Groovy solver on all 5 matrices:
       cd Languages/Groovy && ./runMe.sh ../../Matrices/{1,2,3,4,5}.matrix

    2. Verify iteration counts match exactly:
       - 656, 439269, 98847, 9085, 445778

    3. Update README.md with validation results and JVM notes
  </action>
  <verify>
    grep -o '"iterations": [0-9]*' Languages/Groovy/metrics.json | head -5
  </verify>
  <done>Groovy validated on all 5 matrices</done>
</task>

<task type="auto">
  <name>Task 3: Implement Dart solver</name>
  <files>Languages/Dart/Sudoku.dart, Languages/Dart/runMe.sh, Languages/Dart/README.md</files>
  <action>
    1. Create/fix Sudoku.dart:
       - Dart has C-like syntax with modern features
       - Use List<List<int>> puzzle
       - int count = 0 for iterations
       - void readMatrixFile(String filename)
       - void printPuzzle()
       - bool isValid(int row, int col, int val)
       - bool solve() - brute-force backtracking
         * count++ for every attempt
       - Use Stopwatch() for timing

    2. Create runMe.sh:
       - LANGUAGE="Dart"
       - SOLVER_BINARY="dart run Sudoku.dart"
       - Source ../common.sh
       - check_toolchain dart

    3. Alternative: compile to native
       - dart compile exe Sudoku.dart -o Sudoku
       - SOLVER_BINARY="./Sudoku"

    4. Create README.md
  </action>
  <verify>
    cd Languages/Dart && dart run Sudoku.dart ../../Matrices/1.matrix | grep "Iterations=656"
  </verify>
  <done>Dart solver matches C reference for Matrix 1</done>
</task>

<task type="auto">
  <name>Task 4: Validate Dart on all matrices</name>
  <files>Languages/Dart/metrics.json</files>
  <action>
    1. Run Dart solver on all 5 matrices:
       cd Languages/Dart && ./runMe.sh ../../Matrices/{1,2,3,4,5}.matrix

    2. Verify iteration counts match exactly

    3. Update README.md with validation results
  </action>
  <verify>
    grep -o '"iterations": [0-9]*' Languages/Dart/metrics.json | head -5
  </verify>
  <done>Dart validated on all 5 matrices</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Groovy solver matches C output format and all 5 iteration counts
- [ ] Dart solver matches C output format and all 5 iteration counts
- [ ] Both metrics.json files populated
- [ ] Both README.md files document validation status
- [ ] Both runMe.sh scripts follow common.sh pattern
</verification>

<success_criteria>
- Groovy and Dart fully validated on all 5 matrices
- Both use common.sh benchmark pattern
- Performance captured in metrics
- Documentation complete
</success_criteria>

<output>
After completion, create `.planning/phases/05.1-tier2-languages/05.1-03-SUMMARY.md`
</output>
